{
  "scripts": {
    "spectral:lint": "spectral lint api/openapi.yaml",
    "newman:test": "newman run postman/bd2_collection.json"
  },
  "devDependencies": {
    "@stoplight/spectral-cli": "^6.0.0",
    "newman": "^6.0.0"
  }
}
